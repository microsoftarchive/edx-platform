(function(require) {

    'use strict';

    require([
        'backbone',
        'gettext',
        'jquery',
        'underscore'
    ],
    function( Backbone, gettext, $, _ ) {
        return {};
    });
}(require));